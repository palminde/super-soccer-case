name: 'Pull request'

on:
  pull_request:
    paths-ignore:
      - '**/*.md'
      - 'vscode/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install dependencies
        run: yarn install

      - name: Lint
        run: yarn lint && yarn prettier

      - name: build
        run: yarn build

      - name: Test
        run: yarn test


      
      
